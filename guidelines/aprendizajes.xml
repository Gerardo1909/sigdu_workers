<?xml version="1.0" encoding="UTF-8" ?>
<aprendizajes_compartidos estado="MIGRADO">
  <!--
    ESTE ARCHIVO FUE MIGRADO. No agregues lecciones acá.

    La memoria ahora vive sharded por tipo de tarea en guidelines/memory/:
      - INDEX.xml        índice liviano (1 línea por lección) — leer SIEMPRE
      - cross.xml        transversales (stack canónico, consistencia) — leer SIEMPRE
      - doc-process.xml  roles documentales
      - backend.xml      backend_dev
      - frontend.xml     frontend_dev
      - qa.xml           qa_testing, code_reviewer

    Las 9 lecciones originales (L001–L009) están migradas: L001/L003/L004/L007 en cross.xml,
    el resto en doc-process.xml. El historial completo de este archivo está en git.

    Cada worker lee solo el/los shard(s) que indica su <memoria_relevante>, según core.xml y workflow.xml.
    El loop de destilación (recurrencia>=3 -> plegar al prompt) lo opera roles/curador_memoria.xml.
  -->
</aprendizajes_compartidos>
